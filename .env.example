# Use .env.development (local) or .env.production (live) — see committed files in repo.
#
# Local dev: leave VITE_API_BASE_URL unset; Vite proxies /api -> backend
# VITE_PROXY_TARGET=http://localhost:6969
#
# Production (backend at https://dkgpro.in/api/):
# VITE_API_BASE_URL=https://dkgpro.in/api
